package server.crawler

class CrawlerCommand {
    companion object {
        const val REGISTER_CRAWLER = "REGISTER_CRAWLER"
        const val CRAWL_ENDED = "CRAWL_ENDED"
        const val CRAWL = "CRAWL"
    }
}